spring:
  application:
    name: config-server

management:
  endpoints:
    web:
      exposure:
        include:
          - "*"

config-server:
    greeting: "hello perf"
    default-value: "Mundo"
